# Install NLTK if not already done 
!pip install nltk 
import nltk 
from nltk.util import ngrams 
text = "AI powered systems leverage n-grams for prediction and AI \ 
applies n-grams in machine translation and AI simplifies \ 
sentence analysis using n-grams features." 
words = text.split()

# Create n-grams as lists (not generators) 
unigrams=ngrams(words, 1) 
bigrams = ngrams(words, 2) 
trigrams = ngrams(words, 3) 
quadgrams = ngrams(words, 4) 
# Print all n-grams 
for ngram_list in [unigrams, bigrams, trigrams, quadgrams]:     
  for grams in ngram_list: 
    print(grams)

# USER-DEFINED FUNCTIONS 
def get_unigrams(words): 
    """Return list of unigrams (single words)."""     
   return [(words[i],) for i in range(len(words))] 
  def get_bigrams(words): 
    """Return list of bigrams (pairs of words)."""    
   return [(words[i], words[i+1]) for i in range(len(words)-1)] 
   def get_trigrams(words): 
    """Return list of trigrams (triplets of words)."""     
   return [(words[i], words[i+1], words[i+2]) for i in range(len(words)-2)] 
    def get_ngrams(words, n): 
    """Return list of n-grams (n consecutive  words)."""     
    return [(words[i:i+n]) for i in range(len(words)-n+1)] 
 
# Main program 
text = "AI powered systems leverage n-grams for prediction and AI applies n-grams in machine translation and AI simplifies sentence analysis using n-grams features." #Tokenize (split) the sentence into words 
words = text.split() 

#Generate using user define functions 
unigrams = get_unigrams(words) 
bigrams = get_bigrams(words) 
trigrams = get_trigrams(words) 
quadgrams = get_ngrams(words, 4) 
 
#Print results 
print("Original Text:",text) 
print("\nUnigrams:") 
print(unigrams) 
print("\nBigrams:") 
print(bigrams) 
print("\nTrigrams:") 
print(trigrams) 
print("\nQuadgrams:") 
print(quadgrams) 
 


 

 
